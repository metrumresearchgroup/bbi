# goreleaser.yml
# Build customization
project_name: bbi

build:
  binary: bbi
  goos:
    - windows
    - darwin
    - linux
  goarch:
    - amd64

# goreleaser.yml
brews:
  # Repository to push the tap to.
  -
    github:
      owner: metrumresearchgroup 
      name: homebrew-tap 
