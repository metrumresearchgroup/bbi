box: google/golang
build:
    steps:

      # get dependencies
      - script:
          name: go get
          code: |
            go get -t ./...

      # Test the project
      - script:
          name: go test
          code: go test ./...
